<mah:MetroWindow x:Class="VeterinarianEMS.Views.EmpMainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:mah="clr-namespace:MahApps.Metro.Controls;assembly=MahApps.Metro"
        xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
        mc:Ignorable="d"
        Title="VEMS" Height="700" Width="1000"
        WindowStartupLocation="CenterScreen"
        TextElement.Foreground="{DynamicResource MaterialDesignBody}"
        Background="{DynamicResource MaterialDesignPaper}"
        FontFamily="{DynamicResource MaterialDesignFont}"
        WindowStyle="SingleBorderWindow" 
        AllowsTransparency="False"
        ResizeMode="CanResize">

    <mah:MetroWindow.Resources>
        <ResourceDictionary>
            <ResourceDictionary.MergedDictionaries>
                <!-- Material Design Theme -->
                <materialDesign:BundledTheme BaseTheme="Light" PrimaryColor="Teal" SecondaryColor="Lime"/>
                <ResourceDictionary Source="pack://application:,,,/MaterialDesignThemes.Wpf;component/Themes/MaterialDesign3.Defaults.xaml"/>
            </ResourceDictionary.MergedDictionaries>

            <!-- Sidebar Menu Item Style -->
            <Style TargetType="{x:Type Button}" x:Key="SidebarMenuItem">
                <Setter Property="Background" Value="#30ABA0"/>
                <Setter Property="Foreground" Value="White"/>
                <Setter Property="BorderThickness" Value="0"/>
                <Setter Property="HorizontalContentAlignment" Value="Left"/>
                <Setter Property="Padding" Value="15,10"/>
                <Setter Property="Margin" Value="0,2"/>
                <Setter Property="Cursor" Value="Hand"/>
                <Setter Property="Template">
                    <Setter.Value>
                        <ControlTemplate TargetType="{x:Type Button}">
                            <Border Background="{TemplateBinding Background}" BorderBrush="{TemplateBinding BorderBrush}" BorderThickness="{TemplateBinding BorderThickness}">
                                <ContentPresenter HorizontalAlignment="{TemplateBinding HorizontalContentAlignment}" VerticalAlignment="{TemplateBinding VerticalAlignment}" Margin="{TemplateBinding Padding}" RecognizesAccessKey="True"/>
                            </Border>
                            <ControlTemplate.Triggers>
                                <Trigger Property="IsMouseOver" Value="True">
                                    <Setter Property="Background" Value="Aqua"/>
                                    <Setter Property="Foreground" Value="Black"/>
                                </Trigger>
                                <Trigger Property="IsFocused" Value="True">
                                    <Setter Property="Background" Value="Teal"/>
                                    <Setter Property="Foreground" Value="White"/>
                                </Trigger>
                            </ControlTemplate.Triggers>
                        </ControlTemplate>
                    </Setter.Value>
                </Setter>
            </Style>
        </ResourceDictionary>
    </mah:MetroWindow.Resources>

    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="200"/>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>
        <Grid.RowDefinitions>
            <RowDefinition Height="60"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <!-- Sidebar -->
        <Border Grid.Row="1" Grid.Column="0" Background="#30ABA0">
            <StackPanel>
                <Button Style="{StaticResource SidebarMenuItem}" Tag="Dashboard" Click="DashboardButton_Click">
                    <StackPanel Orientation="Horizontal">
                        <materialDesign:PackIcon Kind="ViewDashboard" Width="20" Height="20" Margin="0,0,10,0"/>
                        <TextBlock Text="Dashboard"/>
                    </StackPanel>
                </Button>

                <Button Style="{StaticResource SidebarMenuItem}" Tag="Attendance" Click="AttendanceButton_Click">
                    <StackPanel Orientation="Horizontal">
                        <materialDesign:PackIcon Kind="CalendarCheck" Width="20" Height="20" Margin="0,0,10,0"/>
                        <TextBlock Text="Attendance"/>
                    </StackPanel>
                </Button>

                <Button Style="{StaticResource SidebarMenuItem}" Tag="Leaves" Click="LeaveRequestButton_Click">
                    <StackPanel Orientation="Horizontal">
                        <materialDesign:PackIcon Kind="CalendarRemove" Width="20" Height="20" Margin="0,0,10,0"/>
                        <TextBlock Text="Leave Requests"/>
                    </StackPanel>
                </Button>

                <!-- NEW: Overtime Requests -->
                <Button Style="{StaticResource SidebarMenuItem}" Tag="Overtime" Click="OvertimeRequestButton_Click">
                    <StackPanel Orientation="Horizontal">
                        <materialDesign:PackIcon Kind="ClockPlus" Width="20" Height="20" Margin="0,0,10,0"/>
                        <TextBlock Text="Overtime Requests"/>
                    </StackPanel>
                </Button>

                <Button Style="{StaticResource SidebarMenuItem}" Tag="Profile" Click="ProfileButton_Click">
                    <StackPanel Orientation="Horizontal">
                        <materialDesign:PackIcon Kind="Account" Width="20" Height="20" Margin="0,0,10,0"/>
                        <TextBlock Text="Profile"/>
                    </StackPanel>
                </Button>

                <!-- NEW: Feedback -->
                <Button Style="{StaticResource SidebarMenuItem}" Tag="Feedback" Click="FeedbackButton_Click">
                    <StackPanel Orientation="Horizontal">
                        <materialDesign:PackIcon Kind="MessageTextOutline" Width="20" Height="20" Margin="0,0,10,0"/>
                        <TextBlock Text="Feedback"/>
                    </StackPanel>
                </Button>
                

                <!-- Separator Line -->
                <Separator Margin="10,337,10,10"
                Background="#444"
                Height="1"
                HorizontalAlignment="Stretch"/>


                <!-- Logout Button at Bottom (slightly lifted up) -->
                <StackPanel VerticalAlignment="Bottom" Margin="0,10,0,10">
                    <Button Style="{StaticResource SidebarMenuItem}" Tag="Logout" Click="LogoutButton_Click">
                        <StackPanel Orientation="Horizontal">
                            <materialDesign:PackIcon Kind="Logout"
                      Width="20"
                      Height="20"
                      Margin="0,0,10,0"/>
                            <TextBlock Text="Logout"/>
                        </StackPanel>
                    </Button>
                </StackPanel>

            </StackPanel>
        </Border>

        <!-- Top Bar -->
        <Border Grid.Row="0" Grid.Column="0" Grid.ColumnSpan="2" Background="#2E2E2E" BorderThickness="0,0,0,1" BorderBrush="{DynamicResource MaterialDesignDivider}">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>

                <!-- EAS Logo/Image -->
                <StackPanel Orientation="Horizontal" VerticalAlignment="Center" Margin="20,0,0,0">
                    <Image Source="Assets/logo.png"
                     Height="35"
                     Width="35"
                     VerticalAlignment="Center"
                     Margin="0,0,8,0"/>
                                    <TextBlock Text="V E M S"
                         FontSize="24"
                         FontWeight="Bold"
                         VerticalAlignment="Center"
                         Foreground="White"/>
                </StackPanel>


                <!-- Right side user info -->
                <StackPanel Grid.Column="1"
                     Orientation="Horizontal"
                     VerticalAlignment="Center"
                     Margin="0,0,15,0">


                    <!-- Name and Role stacked vertically -->
                    <StackPanel Orientation="Vertical" VerticalAlignment="Center">
                        <!-- Employee Name -->
                        <TextBlock Text="{Binding FullName}"
                    Foreground="White"
                    FontWeight="SemiBold"
                    FontSize="14"
                    Margin="0,0,0,2"/>

                        <!-- Employee Role -->
                        <TextBlock Text="{Binding Role}"
                    Foreground="#CCCCCC"
                    FontStyle="Italic"
                    FontSize="12"/>
                    </StackPanel>
                    <!-- User Icon -->
                    <materialDesign:PackIcon Kind="AccountCircle"
                   Width="35"
                   Height="35"
                   VerticalAlignment="Center"
                   Margin="10,0,8,0"
                   Foreground="White"/> 
                </StackPanel>
            </Grid>
        </Border>

        <!-- Main Content Area -->
        <Border Grid.Row="1" Grid.Column="1" Background="#F0F0F0">
            <ContentControl x:Name="MainContentArea" Margin="15"/>
        </Border>
    </Grid>
</mah:MetroWindow>
