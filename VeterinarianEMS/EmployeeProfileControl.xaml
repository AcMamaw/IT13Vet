<UserControl x:Class="VeterinarianEMS.EmployeeProfileControl"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             mc:Ignorable="d"
             d:DesignHeight="700" d:DesignWidth="800">

    <Grid Background="#F0F0F0">
        <ScrollViewer VerticalScrollBarVisibility="Auto">
            <StackPanel HorizontalAlignment="Center" Width="750" Margin="0,20,0,20">

                <!-- PAGE TITLE -->
                <TextBlock Text="Employee Profile" FontSize="24" FontWeight="Bold" Foreground="Black" Margin="0,0,0,10"/>

                <!-- PROFILE CARD -->
                <Border Background="White" CornerRadius="12" Padding="15" Margin="0,0,0,20" Width="750">
                    <Grid>
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="150"/>
                            <ColumnDefinition Width="*" />
                        </Grid.ColumnDefinitions>

                        <!-- PROFILE IMAGE -->
                        <Button Width="130" Height="130" Background="Transparent" BorderThickness="0" Click="ProfileImage_Click">
                            <Grid>
                                <!-- Background circle -->
                                <Ellipse Fill="#DDD" Width="130" Height="130" HorizontalAlignment="Center" VerticalAlignment="Center"/>

                                <!-- Profile Image perfectly inside circle -->
                                <Image x:Name="ProfileImage"
               Width="130" Height="130"
               Stretch="Uniform"
               HorizontalAlignment="Center" VerticalAlignment="Center">
                                    <Image.Clip>
                                        <!-- Clip exactly same as background circle -->
                                        <EllipseGeometry Center="65,65" RadiusX="65" RadiusY="65"/>
                                    </Image.Clip>
                                </Image>
                            </Grid>
                        </Button>



                        <!-- EMPLOYEE INFO -->
                        <StackPanel Grid.Column="1" Margin="15,0,0,0" VerticalAlignment="Top">
                            <TextBlock x:Name="FullNameText" Text="Full Name" FontSize="18" FontWeight="Bold" Foreground="#333"/>
                            <TextBlock x:Name="PositionText" Text="Position: " FontSize="14" Margin="0,3,0,0" Foreground="#555"/>
                            <TextBlock x:Name="EmployeeIDText" Text="Employee ID: " FontSize="14" Margin="0,3,0,0" Foreground="#555"/>
                            <TextBlock x:Name="EmailText" Text="Email: " FontSize="14" Margin="0,3,0,0" Foreground="#555"/>
                            <TextBlock x:Name="PhoneText" Text="Phone: " FontSize="14" Margin="0,3,0,0" Foreground="#555"/>
                            <TextBlock x:Name="DepartmentText" Text="Department: " FontSize="14" Margin="0,3,0,0" Foreground="#555"/>
                            <TextBlock x:Name="HireDateText" Text="Hire Date: " FontSize="14" Margin="0,3,0,0" Foreground="#555"/>
                        </StackPanel>
                    </Grid>
                </Border>

                <!-- PAYSLIP TABLE -->
                <Border Background="White" CornerRadius="12" Padding="15" Margin="0,0,0,0">
                    <StackPanel>
                        <TextBlock Text="Payslip History" FontSize="18" FontWeight="Bold" Margin="0,0,0,10"/>

                        <!-- Search -->
                        <StackPanel Orientation="Horizontal" Margin="0,0,0,10">
                            <TextBlock Text="Search:" VerticalAlignment="Center" Margin="0,0,5,0"/>
                            <TextBox x:Name="PayslipSearchTextBox" Width="200" Height="25" TextChanged="PayslipSearchTextBox_TextChanged"/>
                        </StackPanel>

                        <!-- DataGrid -->
                        <DataGrid x:Name="PayslipDataGrid" AutoGenerateColumns="False" IsReadOnly="True" Height="250">
                            <DataGrid.Columns>
                                <DataGridTextColumn Header="Pay Period Start" Binding="{Binding PayPeriodStart, StringFormat=d}" Width="*"/>
                                <DataGridTextColumn Header="Pay Period End" Binding="{Binding PayPeriodEnd, StringFormat=d}" Width="*"/>
                                <DataGridTextColumn Header="Total Hours Worked" Binding="{Binding TotalHoursWorked}" Width="*"/>
                                <DataGridTextColumn Header="Leave Days" Binding="{Binding LeaveDays}" Width="*"/>
                                <DataGridTextColumn Header="Gross Pay" Binding="{Binding GrossPay, StringFormat=C}" Width="*"/>
                            </DataGrid.Columns>
                        </DataGrid>
                    </StackPanel>
                </Border>

            </StackPanel>
        </ScrollViewer>
    </Grid>
</UserControl>
