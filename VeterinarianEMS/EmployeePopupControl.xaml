<UserControl x:Class="VeterinarianEMS.Views.EmployeePopupControl"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             Width="Auto" Height="720"
             Background="Transparent"
             Loaded="UserControl_Loaded">

    <UserControl.Resources>
        <!-- ✅ TextBox Style -->
        <Style x:Key="CustomTextBox" TargetType="TextBox">
            <Setter Property="Margin" Value="0,0,0,10"/>
            <Setter Property="Padding" Value="8"/>
            <Setter Property="Height" Value="34"/>
            <Setter Property="BorderBrush" Value="#30ABA0"/>
            <Setter Property="BorderThickness" Value="1.3"/>
            <Setter Property="Background" Value="White"/>
            <Setter Property="Foreground" Value="#333"/>
            <Setter Property="FontSize" Value="14"/>
            <Setter Property="VerticalContentAlignment" Value="Center"/>
        </Style>

        <!-- ✅ ComboBox Style -->
        <Style x:Key="CustomComboBox" TargetType="ComboBox">
            <Setter Property="Margin" Value="0,0,0,10"/>
            <Setter Property="Height" Value="34"/>
            <Setter Property="Padding" Value="6"/>
            <Setter Property="BorderBrush" Value="#30ABA0"/>
            <Setter Property="BorderThickness" Value="1.3"/>
            <Setter Property="Background" Value="White"/>
            <Setter Property="Foreground" Value="#333"/>
            <Setter Property="FontSize" Value="14"/>
        </Style>

        <!-- ✅ DatePicker Style -->
        <Style x:Key="CustomDatePicker" TargetType="DatePicker">
            <Setter Property="Margin" Value="0,0,0,10"/>
            <Setter Property="Height" Value="34"/>
            <Setter Property="Padding" Value="6"/>
            <Setter Property="BorderBrush" Value="#30ABA0"/>
            <Setter Property="BorderThickness" Value="1.3"/>
            <Setter Property="Background" Value="White"/>
            <Setter Property="Foreground" Value="#333"/>
            <Setter Property="FontSize" Value="14"/>
        </Style>

        <!-- ✅ Primary Button Style -->
        <Style x:Key="PrimaryButton" TargetType="Button">
            <Setter Property="Height" Value="34"/>
            <Setter Property="Width" Value="100"/>
            <Setter Property="Background" Value="#30ABA0"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Margin" Value="5"/>
            <Setter Property="FontWeight" Value="SemiBold"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border x:Name="border"
                                Background="{TemplateBinding Background}"
                                CornerRadius="4"
                                BorderThickness="{TemplateBinding BorderThickness}"
                                BorderBrush="{TemplateBinding BorderBrush}">
                            <ContentPresenter HorizontalAlignment="Center"
                                              VerticalAlignment="Center"
                                              Margin="{TemplateBinding Padding}"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="border" Property="Background" Value="#259C90"/>
                            </Trigger>
                            <Trigger Property="IsPressed" Value="True">
                                <Setter TargetName="border" Property="Background" Value="#1E7F75"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <!-- ✅ Secondary Button Style -->
        <Style x:Key="SecondaryButton" TargetType="Button">
            <Setter Property="Height" Value="34"/>
            <Setter Property="Width" Value="100"/>
            <Setter Property="Background" Value="Transparent"/>
            <Setter Property="Foreground" Value="#333"/>
            <Setter Property="BorderBrush" Value="#CCC"/>
            <Setter Property="BorderThickness" Value="1.3"/>
            <Setter Property="FontWeight" Value="SemiBold"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border x:Name="border"
                                Background="{TemplateBinding Background}"
                                CornerRadius="4"
                                BorderThickness="{TemplateBinding BorderThickness}"
                                BorderBrush="{TemplateBinding BorderBrush}">
                            <ContentPresenter HorizontalAlignment="Center"
                                              VerticalAlignment="Center"
                                              Margin="{TemplateBinding Padding}"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="border" Property="BorderBrush" Value="#30ABA0"/>
                            </Trigger>
                            <Trigger Property="IsPressed" Value="True">
                                <Setter TargetName="border" Property="Background" Value="#F0F0F0"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
    </UserControl.Resources>

    <!-- ✅ Main Layout (no ScrollViewer) -->
    <Grid Margin="10">
        <Border Background="White" CornerRadius="10" Padding="20"
                BorderBrush="#E0E0E0" BorderThickness="1"
                HorizontalAlignment="Center" VerticalAlignment="Center"
                Width="720">

            <StackPanel>
                <!-- Header -->
                <TextBlock Text="Add New Employee"
                   FontSize="22"
                   FontWeight="SemiBold"
                   Foreground="#30ABA0"
                   Margin="0,0,0,20"
                   TextAlignment="Center"
                   HorizontalAlignment="Center"/>

                <!-- Personal Info -->
                <TextBlock Text="Personal Information"
                           FontSize="16" FontWeight="Bold"
                           Foreground="#333" Margin="0,0,0,10"/>

                <Grid Margin="0,0,0,10">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="*"/>
                    </Grid.ColumnDefinitions>

                    <StackPanel Grid.Column="0">
                        <TextBlock Text="First Name" Margin="0,0,0,2"/>
                        <TextBox x:Name="FirstNameTextBox" Style="{StaticResource CustomTextBox}" Width="220"/>
                    </StackPanel>

                    <StackPanel Grid.Column="1">
                        <TextBlock Text="Middle Name" Margin="0,0,0,2"/>
                        <TextBox x:Name="MiddleNameTextBox" Style="{StaticResource CustomTextBox}" Width="220"/>
                    </StackPanel>

                    <StackPanel Grid.Column="2">
                        <TextBlock Text="Last Name" Margin="0,0,0,2"/>
                        <TextBox x:Name="LastNameTextBox" Style="{StaticResource CustomTextBox}" Width="220"/>
                    </StackPanel>
                </Grid>

                <Grid Margin="0,0,0,10">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="*"/>
                    </Grid.ColumnDefinitions>

                    <StackPanel Grid.Column="0">
                        <TextBlock Text="Sex" Margin="0,0,0,2"/>
                        <ComboBox x:Name="SexComboBox" Style="{StaticResource CustomComboBox}" Width="220">
                            <ComboBoxItem Content="Male"/>
                            <ComboBoxItem Content="Female"/>
                        </ComboBox>
                    </StackPanel>

                    <StackPanel Grid.Column="1">
                        <TextBlock Text="Email" Margin="0,0,0,2"/>
                        <TextBox x:Name="EmailTextBox" Style="{StaticResource CustomTextBox}" TextChanged="EmailTextBox_TextChanged" Width="220"/>
                    </StackPanel>

                    <StackPanel Grid.Column="2">
                        <TextBlock Text="Date of Birth" Margin="0,0,0,2"/>
                        <DatePicker x:Name="DOBPicker" Style="{StaticResource CustomDatePicker}" Width="220"/>
                    </StackPanel>
                </Grid>

                <Grid Margin="0,0,0,10">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="2*"/>
                        <ColumnDefinition Width="*"/>
                    </Grid.ColumnDefinitions>

                    <StackPanel Grid.Column="0">
                        <TextBlock Text="Contact Number" Margin="0,0,0,2"/>
                        <TextBox x:Name="ContactTextBox" Style="{StaticResource CustomTextBox}" Width="170"/>
                    </StackPanel>

                    <StackPanel Grid.Column="1">
                        <TextBlock Text="Address" Margin="0,0,0,2"/>
                        <TextBox x:Name="AddressTextBox" Style="{StaticResource CustomTextBox}" Height="34" Width="325"/>
                    </StackPanel>

                    <StackPanel Grid.Column="2">
                        <TextBlock Text="Hire Date" Margin="0,0,0,2"/>
                        <DatePicker x:Name="HireDatePicker" Style="{StaticResource CustomDatePicker}" Width="170"/>
                    </StackPanel>
                </Grid>

                <!-- Job Info -->
                <TextBlock Text="Job Information"
                           FontSize="16" FontWeight="Bold"
                           Foreground="#333" Margin="20,10,0,10"/>

                <Grid Margin="0,0,0,10">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="*"/>
                    </Grid.ColumnDefinitions>

                    <StackPanel Grid.Column="0">
                        <TextBlock Text="Department" Margin="0,0,0,2" Width="316"/>
                        <ComboBox x:Name="DepartmentComboBox" Style="{StaticResource CustomComboBox}" Width="320"/>
                    </StackPanel>

                    <StackPanel Grid.Column="1">
                        <TextBlock Text="Position" Margin="0,0,0,2" Width="316"/>
                        <ComboBox x:Name="PositionComboBox" Style="{StaticResource CustomComboBox}" Width="320"/>
                    </StackPanel>
                </Grid>

                <Grid Margin="0,0,0,10">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="*"/>
                    </Grid.ColumnDefinitions>

                    <StackPanel Grid.Column="0">
                        <TextBlock Text="Hourly Rate" Margin="0,0,0,2" Width="318"/>
                        <TextBox x:Name="HourlyRateTextBox" Style="{StaticResource CustomTextBox}" TextChanged="HourlyRateTextBox_TextChanged" Width="320"/>
                    </StackPanel>

                    <StackPanel Grid.Column="1">
                        <TextBlock Text="Base Salary" Margin="0,0,0,2" Width="319"/>
                        <TextBox x:Name="BaseSalaryTextBox" Style="{StaticResource CustomTextBox}" IsReadOnly="True" Width="320"/>
                    </StackPanel>
                </Grid>

                <!-- Account Info -->
                <TextBlock Text="Account Information"
                           FontSize="16" FontWeight="Bold"
                           Foreground="#333" Margin="20,10,0,10"/>

                <Grid Margin="0,0,0,10">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="1.5*"/>
                        <ColumnDefinition Width="1.5*"/>
                        <ColumnDefinition Width="Auto"/>
                    </Grid.ColumnDefinitions>

                    <StackPanel Grid.Column="0">
                        <TextBlock Text="Username" Margin="0,0,0,2" Width="267"/>
                        <TextBox x:Name="UsernameTextBox" Style="{StaticResource CustomTextBox}" Width="270"/>
                    </StackPanel>

                    <StackPanel Grid.Column="1">
                        <TextBlock Text="Password" Margin="0,0,0,2" Width="266"/>
                        <TextBox x:Name="PasswordTextBox" Style="{StaticResource CustomTextBox}" Width="270"/>
                    </StackPanel>

                    <StackPanel Grid.Column="2" VerticalAlignment="Bottom" Margin="10,0,0,10">
                        <Button Content="Generate" Width="90" Height="34"
                                Background="#30ABA0" Foreground="White"
                                BorderThickness="0" Cursor="Hand"
                                Click="GeneratePassword_Click"/>
                    </StackPanel>
                </Grid>

                <!-- ✅ Buttons -->
                <StackPanel Orientation="Horizontal" HorizontalAlignment="Center" Margin="0,20,0,0">
                    <Button Content="Save" Style="{StaticResource PrimaryButton}" Click="Save_Click"/>
                    <Button Content="Cancel" Style="{StaticResource SecondaryButton}" Click="Cancel_Click"/>
                </StackPanel>
            </StackPanel>
        </Border>
    </Grid>
</UserControl>
