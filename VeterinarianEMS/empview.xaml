<UserControl x:Class="VeterinarianEMS.Controls.EmpView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             Width="480" Height="630" Background="Transparent">
    <UserControl.Resources>

        <Style TargetType="TextBlock" x:Key="TitleStyle">
            <Setter Property="FontSize" Value="20"/>
            <Setter Property="FontWeight" Value="Bold"/>
            <Setter Property="Foreground" Value="#30ABA0"/>
            <Setter Property="Margin" Value="0,0,0,15"/>
            <Setter Property="HorizontalAlignment" Value="Center"/>
        </Style>

        <Style TargetType="TextBlock" x:Key="HeaderStyle">
            <Setter Property="FontSize" Value="15"/>
            <Setter Property="FontWeight" Value="SemiBold"/>
            <Setter Property="Foreground" Value="#30ABA0"/>
            <Setter Property="Margin" Value="0,0,0,10"/>
        </Style>

        <Style TargetType="TextBlock" x:Key="LabelStyle">
            <Setter Property="FontWeight" Value="Bold"/>
            <Setter Property="Foreground" Value="#333333"/>
            <Setter Property="Margin" Value="0,6,0,0"/>
        </Style>

        <Style TargetType="TextBlock" x:Key="ValueStyle">
            <Setter Property="FontSize" Value="13"/>
            <Setter Property="Foreground" Value="#555555"/>
            <Setter Property="Margin" Value="0,2,0,5"/>
            <Setter Property="TextWrapping" Value="Wrap"/>
        </Style>

        <Style TargetType="Button" x:Key="PrimaryButtonStyle">
            <Setter Property="Width" Value="100"/>
            <Setter Property="Height" Value="32"/>
            <Setter Property="HorizontalAlignment" Value="Center"/>
            <Setter Property="Background" Value="#30ABA0"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="FontSize" Value="14"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border Background="{TemplateBinding Background}"
                                CornerRadius="4">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter Property="Background" Value="#38C0B2"/>
                            </Trigger>
                            <Trigger Property="IsPressed" Value="True">
                                <Setter Property="Background" Value="#259489"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style TargetType="TabControl">
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Padding" Value="0"/>
            <Setter Property="Margin" Value="0,0,0,0"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="{x:Type TabControl}">
                        <Grid ClipToBounds="true" SnapsToDevicePixels="true" KeyboardNavigation.TabNavigation="Local">
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition x:Name="ColumnDefinition0"/>
                                <ColumnDefinition x:Name="ColumnDefinition1" Width="0"/>
                            </Grid.ColumnDefinitions>
                            <Grid.RowDefinitions>
                                <RowDefinition x:Name="RowDefinition0" Height="Auto"/>
                                <RowDefinition x:Name="RowDefinition1" Height="*"/>
                            </Grid.RowDefinitions>
                            <TabPanel x:Name="HeaderPanel" IsItemsHost="true" Margin="2,2,2,0"
                                      Background="Transparent" Grid.Column="0" Grid.Row="0"/>
                            <Border x:Name="ContentBorder" BorderBrush="{TemplateBinding BorderBrush}"
                                    BorderThickness="{TemplateBinding BorderThickness}"
                                    Background="{TemplateBinding Background}"
                                    Grid.Column="0" Grid.Row="1"
                                    CornerRadius="0,8,8,8"
                                    Padding="10">
                                <ContentPresenter x:Name="PART_SelectedContentHost"
                                                  ContentSource="SelectedContent"
                                                  Margin="{TemplateBinding Padding}"
                                                  SnapsToDevicePixels="{TemplateBinding SnapsToDevicePixels}"/>
                            </Border>
                        </Grid>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style TargetType="TabItem">
            <Setter Property="FocusVisualStyle" Value="{x:Null}"/>
            <Setter Property="Background" Value="#EAEAEA"/>
            <Setter Property="Foreground" Value="#666666"/>
            <Setter Property="BorderBrush" Value="#E0E0E0"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="Padding" Value="10,5,10,5"/>
            <Setter Property="Margin" Value="0,0,2,0"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="{x:Type TabItem}">
                        <Grid SnapsToDevicePixels="true">
                            <Border x:Name="Border"
                                    Background="{TemplateBinding Background}"
                                    BorderBrush="{TemplateBinding BorderBrush}"
                                    BorderThickness="{TemplateBinding BorderThickness}"
                                    CornerRadius="6,6,0,0"
                                    Margin="0,0,0,-1">
                                <ContentPresenter x:Name="ContentSite"
                                                  ContentSource="Header"
                                                  HorizontalAlignment="Center"
                                                  VerticalAlignment="Center"
                                                  TextElement.Foreground="{TemplateBinding Foreground}"
                                                  Margin="{TemplateBinding Padding}"
                                                  SnapsToDevicePixels="{TemplateBinding SnapsToDevicePixels}"/>
                            </Border>
                        </Grid>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="true">
                                <Setter TargetName="Border" Property="Background" Value="#F0F0F0"/>
                                <Setter Property="Foreground" Value="#333333"/>
                            </Trigger>
                            <Trigger Property="IsSelected" Value="true">
                                <Setter Property="Panel.ZIndex" Value="1"/>
                                <Setter TargetName="Border" Property="Background" Value="White"/>
                                <Setter TargetName="Border" Property="BorderBrush" Value="#E0E0E0"/>
                                <Setter TargetName="Border" Property="BorderThickness" Value="1,1,1,0"/>
                                <Setter Property="Foreground" Value="#30ABA0"/>
                                <Setter Property="FontWeight" Value="Bold"/>
                            </Trigger>
                            <Trigger Property="IsEnabled" Value="false">
                                <Setter Property="Foreground" Value="#CCCCCC"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

    </UserControl.Resources>

    <Grid Margin="10">
        <Border Background="White" CornerRadius="10" Padding="15"
                BorderBrush="#E0E0E0" BorderThickness="1"
                HorizontalAlignment="Stretch" VerticalAlignment="Stretch">

            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="*"/>
                    <RowDefinition Height="Auto"/>
                </Grid.RowDefinitions>

                <StackPanel Grid.Row="0" HorizontalAlignment="Center" VerticalAlignment="Top">
                    <!-- Title -->
                    <TextBlock Text="Employee View" Style="{StaticResource TitleStyle}"/>

                    <!-- Rate Me Label -->
                    <TextBlock Text="Rate Me!!"
               FontSize="14"
               FontWeight="SemiBold"
               Foreground="#555555"
               HorizontalAlignment="Center"
               Margin="0,5,0,0"/>

                    <!-- Stars -->
                    <StackPanel x:Name="StarPanel" Orientation="Horizontal"
                HorizontalAlignment="Center"
                Margin="0,2,0,15">
                        <TextBlock Text="★" FontSize="50" Foreground="Gray" Margin="5,0"
                   MouseLeftButtonDown="Star_Click" Tag="1"/>
                        <TextBlock Text="★" FontSize="50" Foreground="Gray" Margin="5,0"
                   MouseLeftButtonDown="Star_Click" Tag="2"/>
                        <TextBlock Text="★" FontSize="50" Foreground="Gray" Margin="5,0"
                   MouseLeftButtonDown="Star_Click" Tag="3"/>
                        <TextBlock Text="★" FontSize="50" Foreground="Gray" Margin="5,0"
                   MouseLeftButtonDown="Star_Click" Tag="4"/>
                        <TextBlock Text="★" FontSize="50" Foreground="Gray" Margin="5,0"
                   MouseLeftButtonDown="Star_Click" Tag="5"/>
                    </StackPanel>
                </StackPanel>


                <TabControl Grid.Row="1">
                    <TabItem Header="Employee">
                        <ScrollViewer VerticalScrollBarVisibility="Auto" Padding="5,10,5,10">
                            <StackPanel>
                                <TextBlock Text="Employee Description" Style="{StaticResource HeaderStyle}"/>

                                <TextBlock Text="Full Name:" Style="{StaticResource LabelStyle}"/>
                                <TextBlock x:Name="FullNameTextBlock" Style="{StaticResource ValueStyle}"/>

                                <TextBlock Text="Sex:" Style="{StaticResource LabelStyle}"/>
                                <TextBlock x:Name="SexTextBlock" Style="{StaticResource ValueStyle}"/>

                                <TextBlock Text="Age:" Style="{StaticResource LabelStyle}"/>
                                <TextBlock x:Name="AgeTextBlock" Style="{StaticResource ValueStyle}"/>

                                <TextBlock Text="Contact Number:" Style="{StaticResource LabelStyle}"/>
                                <TextBlock x:Name="ContactTextBlock" Style="{StaticResource ValueStyle}"/>

                                <TextBlock Text="Address:" Style="{StaticResource LabelStyle}"/>
                                <TextBlock x:Name="AddressTextBlock" Style="{StaticResource ValueStyle}"/>

                                <TextBlock Text="Date of Birth:" Style="{StaticResource LabelStyle}"/>
                                <TextBlock x:Name="DOBTextBlock" Style="{StaticResource ValueStyle}"/>
                            </StackPanel>
                        </ScrollViewer>
                    </TabItem>

                    <TabItem Header="Job">
                        <ScrollViewer VerticalScrollBarVisibility="Auto" Padding="5,10,5,10">
                            <StackPanel>
                                <TextBlock Text="Job Description" Style="{StaticResource HeaderStyle}"/>

                                <TextBlock Text="Position:" Style="{StaticResource LabelStyle}"/>
                                <TextBlock x:Name="PositionTextBlock" Style="{StaticResource ValueStyle}"/>

                                <TextBlock Text="Department:" Style="{StaticResource LabelStyle}"/>
                                <TextBlock x:Name="DepartmentTextBlock" Style="{StaticResource ValueStyle}"/>

                                <TextBlock Text="Base Salary:" Style="{StaticResource LabelStyle}"/>
                                <TextBlock x:Name="SalaryTextBlock" Style="{StaticResource ValueStyle}"/>

                                <TextBlock Text="Hire Date:" Style="{StaticResource LabelStyle}"/>
                                <TextBlock x:Name="HireDateTextBlock" Style="{StaticResource ValueStyle}"/>
                            </StackPanel>
                        </ScrollViewer>
                    </TabItem>

                    <TabItem Header="Schedule">
                        <ScrollViewer VerticalScrollBarVisibility="Auto" Padding="5,10,5,10">
                            <StackPanel>
                                <TextBlock Text="Shift Description" Style="{StaticResource HeaderStyle}"/>

                                <TextBlock Text="Shift Name:" Style="{StaticResource LabelStyle}"/>
                                <TextBlock x:Name="ShiftNameTextBlock" Style="{StaticResource ValueStyle}"/>

                                <TextBlock Text="Start Time:" Style="{StaticResource LabelStyle}"/>
                                <TextBlock x:Name="ShiftStartTextBlock" Style="{StaticResource ValueStyle}"/>

                                <TextBlock Text="End Time:" Style="{StaticResource LabelStyle}"/>
                                <TextBlock x:Name="ShiftEndTextBlock" Style="{StaticResource ValueStyle}"/>

                                <TextBlock Text="Days:" Style="{StaticResource LabelStyle}"/>
                                <TextBlock x:Name="ShiftDaysTextBlock" Style="{StaticResource ValueStyle}"/>
                            </StackPanel>
                        </ScrollViewer>
                    </TabItem>
                </TabControl>
                <!-- Buttons -->
                <StackPanel Grid.Row="2" Orientation="Horizontal" HorizontalAlignment="Center" Margin="0,10,0,10">
                    <Button x:Name="SubmitButton" Style="{StaticResource PrimaryButtonStyle}" Content="Submit" Width="120" Height="36" Margin="0,0,10,0" Click="SubmitButton_Click" Visibility="Collapsed"/>
                    <Button x:Name="CancelButton" Style="{StaticResource PrimaryButtonStyle}" Content="Cancel" Width="120" Height="36" Click="CancelButton_Click" Visibility="Collapsed"/>
                    <Button x:Name="CloseButton" Style="{StaticResource PrimaryButtonStyle}" Content="Close" Width="120" Height="36" Margin="10,0,0,0" Click="CloseButton_Click"/>
                </StackPanel>

            </Grid>
        </Border>
    </Grid>
</UserControl>
